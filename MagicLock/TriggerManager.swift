import SwiftUI

// TriggerManager is now minimal.
// The main hidden trigger is the triple-tap on the time in LockScreenView.
// This file is kept for future expansion (e.g., invisible quadrant gestures,
// shake-to-trigger, volume-button sequences, etc.)

// No overlay is needed — the triple-tap on the clock handles settings access,
// and the auto-unlock can be triggered from the settings panel.
